[package]
name = "ssp-protocol"
version = "0.0.1-canary.276"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
blake3 = { version = "1.5", default-features = false, features = ["std"] }
