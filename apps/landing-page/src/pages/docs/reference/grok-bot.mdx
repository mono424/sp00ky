---
layout: ../../../layouts/DocsLayout.astro
title: Grok Bot marketplace recipe
description: Pack Sp00ky's scrubbed Grok Bot template from apps/grok-bot — skills for app builders and production operators.
---

import CodeBlock from '../../../components/ui/CodeBlock.astro';
import Note from '../../../components/ui/Note.astro';
import CardGrid from '../../../components/ui/CardGrid.astro';
import LinkCard from '../../../components/ui/LinkCard.astro';

export const mcpInstall = `{
  "mcpServers": {
    "spooky-devtools": {
      "command": "npx",
      "args": ["-y", "@spooky-sync/devtools-mcp"]
    }
  }
}`;

export const verifyTree = `find apps/grok-bot -type f | sort`;

The monorepo ships a **Grok Bot marketplace recipe** under [`apps/grok-bot`](https://github.com/mono424/sp00ky/tree/main/apps/grok-bot). It is the source of truth for a **public** Grok Bot template that helps people build and operate Sp00ky (`@spooky-sync`) apps.

This folder is not a runtime npm library. It holds scrubbed profile copy, memories, and skills you pack into Grok Bot.

## Who it is for

- **App builders** starting a Sp00ky project (schema → codegen → Solid clients → SSP)
- **Operators** running Sp00ky in production (doctor/verify, allowlists, SSP/scheduler health, incident triage)
- **Contributors** working in this monorepo

## What ships in the recipe

Skills live under `apps/grok-bot/skills/*/SKILL.md`:

| Skill folder | Use when |
| :--- | :--- |
| `sp00ky-onboarding` | First import, explain Sp00ky, pick app vs prod vs monorepo track |
| `sp00ky-schema-codegen` | Schema edits, `spky generate`, `spky doctor`, allowlists, `@crdt` / `@parent` |
| `sp00ky-solid-app` | Solid providers, queries/mutations, Solid 2 bindings |
| `sp00ky-debug-mcp` | Stale queries, sync stalls, live schema/data via DevTools MCP |
| `sp00ky-prod-ops` | Production health, deploys, drift, backups — diagnose before changing anything |
| `sp00ky-monorepo` | Contributing here: docs-sync, version bumps, PR hygiene |

Storefront copy and publish notes live in [`apps/grok-bot/BOT.md`](https://github.com/mono424/sp00ky/blob/main/apps/grok-bot/BOT.md).

## Pack as a public Grok Bot template

1. Keep skills and memories aligned with real CLI behaviour (`spky generate`, `spky doctor`, `spky recipe`, `spky agents init`, `sp00ky.yml`).
2. In Grok Bot, stage a **public** template from these recipes (profile + skills + memories).
3. Attach `sp00ky-onboarding` as the getting-started skill only if that skill already exists on the bot before packing.
4. Review the publish card: no secrets, no private machine paths, no customer data.

<Note title="DevTools MCP is not a marketplace plugin">
  Grok Bot public templates only pack marketplace connectors. Importers wire Sp00ky DevTools MCP themselves:
</Note>

<CodeBlock code={mcpInstall} lang="json" />

Or from an app: `spky mcp`. See [AI coding agents](/docs/reference/ai-agents) and [MCP server](/docs/reference/mcp) for how DevTools MCP relates to cloud and scheduler admin MCP.

## Verify in the monorepo

<CodeBlock code={verifyTree} lang="bash" />

After docs edits, build the site:

```bash
cd apps/landing-page && pnpm build
```

## Next steps

<CardGrid>
  <LinkCard
    href="/docs/reference/ai-agents"
    title="AI coding agents"
    description="AGENTS.md, recipes, doctor loop, and DevTools MCP."
  />
  <LinkCard
    href="/docs/reference/mcp"
    title="MCP server"
    description="Cloud and scheduler admin MCP for deployed projects."
  />
  <LinkCard
    href="/docs/reference/cli"
    title="CLI"
    description="spky generate, doctor, recipe, agents, and deploy commands."
  />
  <LinkCard
    href="/docs/reference/devtools"
    title="DevTools extension"
    description="Chrome panel for live queries, cache, and client events."
  />
</CardGrid>
